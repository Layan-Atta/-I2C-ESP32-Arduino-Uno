# 🤖 مشروع اتصال I2C بين ESP32 و Arduino Uno

هذا المشروع يوضح كيفية إنشاء اتصال تسلسلي بين لوحة **ESP32** (الرئيسي - Master) ولوحة **Arduino Uno** (التابع - Slave) باستخدام بروتوكول **I2C**. الهدف هو أن ترسل ESP32 أمراً (تشغيل/إطفاء) إلى Arduino، ليتحكم الأردوينو بضوء LED.

![b](bb)

---

## 💡 نظرة عامة على البروتوكول

**I2C (Inter-Integrated Circuit):** هو بروتوكول اتصال ثنائي الاتجاه يسمح بوجود أكثر من جهاز رئيسي (Master) وأكثر من جهاز تابع (Slave) على نفس الخطوط. يستخدم سلكين فقط:

1.  **SCL (Serial Clock):** خط الساعة الذي يحدد توقيت نقل البيانات.
2.  **SDA (Serial Data):** خط البيانات الذي يتم عبره نقل المعلومات.

---

## 🛠️ المكونات المطلوبة (Hardware)

* **الرئيسي (Master):** لوحة ESP32 (منفذ IO21 و IO22).
* **التابع (Slave):** لوحة Arduino Uno.
* ضوء LED واحد.
* مقاومة 220 أوم للـ LED.
* لوحة تجارب (Breadboard).
* أسلاك توصيل (Jumper Wires).

---

## 🔌 التوصيلات الكهربائية (Wiring Diagram)

| المنفذ في ESP32 (Master) | المنفذ في Arduino Uno (Slave) | الوظيفة |
| :----------------------- | :----------------------------- | :------- |
| **GND** | **GND** | أرضي مشترك (ضروري) |
| **IO21 (SDA)** | **A4 (SDA)** | خط البيانات |
| **IO22 (SCL)** | **A5 (SCL)** | خط الساعة |
| -                        | **Digital Pin 13** | مخرج LED (عبر مقاومة 220Ω) |

---

## 💻 الأكواد البرمجية (Source Code)

يتكون المشروع من كودين رئيسيين (يجب استخدام مكتبة `Wire.h` في كليهما):

### 1. الكود التابع (Arduino Uno)

* **العنوان:** يستخدم العنوان `0x08` لتعريف لوحة Arduino كجهاز تابع.
* **الوظيفة:** يستقبل البيانات المرسلة من ESP32 عبر دالة `Wire.onReceive()`.
    * إذا استقبل حرف `'1'`، يشغل LED المتصل بالمنفذ 13.
    * إذا استقبل حرف `'0'`، يطفئ LED المتصل بالمنفذ 13.

```cpp
#include <Wire.h>
// ...
```

### 2. الكود الرئيسي (ESP32)
المنافذ: يتم تعريف منافذ الاتصال على GPIO 21 (SDA) و GPIO 22 (SCL).

الوظيفة: يرسل الأوامر بالتناوب إلى العنوان 0x08 الخاص بالأردوينو.

يرسل '1' (تشغيل) ثم ينتظر 2000 مللي ثانية.

يرسل '0' (إطفاء) ثم ينتظر 2000 مللي ثانية.

```
#include <Wire.h>
// ... (الكود الكامل للـ ESP32)
```


### ▶️ خطوات التشغيل
التوصيل: قم بتوصيل المكونات كما هو موضح في جدول التوصيلات.

رفع كود Arduino: اختر لوحة Arduino Uno وقم برفع الكود التابع (Slave Code).

رفع كود ESP32: اختر لوحة ESP32 Dev Module وقم برفع الكود الرئيسي (Master Code).

المراقبة: يجب أن يبدأ ضوء LED المتصل بلوحة Arduino بالوميض (تشغيل ثانيتين، إطفاء ثانيتين)، مما يؤكد نجاح الاتصال والتحكم.
